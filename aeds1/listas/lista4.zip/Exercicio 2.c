#include <stdio.h>
#include <math.h>

int salario(){
    int n, n1;
    char media ;

    media = n + n1 / 2;

    printf("\nMedia salarial: %d", media);
    

    return 0;
}

int main(){
    int a,b;
    printf("\nDigite 2 salarios: ");
    scanf("%d %d", &a, &b);

    salario (a, b);

    return media;
}

