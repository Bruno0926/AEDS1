#include <stdio.h>
#include <math.h>

int main()
{
    int ano;
    
    printf("\nDigite um ano qualquer: ");
    scanf("%i", &ano);

    {if} (ano / 100 = 0 && ano / 4 = 0);
         (ano / 400 = 0);
    
    printf("\num ano bissexto");

return 0;

}

// nao consegui identificar o erro do codigo