#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    float valor, promo;
    int op, on ,d;
    
    printf("\nDigite o valor da diaria (100): ");
    scanf("%f", &valor);

    
    if (promo = valor * 0.75)
        printf("\nO valor da diaria promocional: %.2f", promo);
    else
        if (op = promo * 60)
            printf("\nO valor arrecadado com 80 por cento de lotacao e diaria promocional: %.2i", op);
        else    
            if (on = 100 * 37.5)
                printf("\nO valor total arrecadado com 50 por cento de ocupação e diária normal: %.2i", on);
            else    
                if (d = op - on)
                    printf("\nDiferenca entre os dois vetores: %.2i", d);
                else
return 0;
}
